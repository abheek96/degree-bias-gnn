model:
  name: GCN
  hidden_dim: 128
  num_layers: 3
  dropout: 0.5

train:
  lr: 0.01
  weight_decay: 5e-4
  epochs: 10000
  patience: 200

plot:
  layer_values: [2, 3, 4, 5, 6]
  compare_models: [GCN]
