model:
  name: GCN
  hidden_dim: 512
  num_layers: 3
  dropout: 0.7

train:
  lr: 0.001
  weight_decay: 5e-4
  epochs: 500
  patience: 50

plot:
  layer_values: [2, 3, 4, 5, 6]
  compare_models: [GCN]
